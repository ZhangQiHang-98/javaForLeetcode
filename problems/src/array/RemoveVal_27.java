package array;

public class RemoveVal_27 {
    /*    public int removeElement(int[] nums, int val) {
            // 要从头开始比较是否相同
            // 要保证sp指针前面的值是完全对的，所以当不等于（不用移除）的时候才会对sp进行操作
            // 要把sp,fp对应的当作一个新的数组来考虑会好一点
            int sp = 0;
            if (nums == null || nums.length == 0) return nums.length;
            for (int fp = 0; fp < nums.length; fp++) {
                if(nums[fp] != val){
                    // 当fp不等于target时，让sp往前移动
                    nums[sp] = nums[fp];
                    sp++;
                }
            }
            return sp;
        }*/
}
